<!DOCTYPE html>
<html lang="pt-BR" data-theme="<%= theme %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Notícias</title>
  <link rel="stylesheet" href="/styles.css">

  <style>
    :root {
      --bg-page: #f5f5f5;
      --text-color: #000;
      --card-bg: #fff;
      --input-bg: #fff;
      --input-border: #ccc;
      --brand-color: #007bff;
      --button-bg: #007bff;
      --button-text: #fff;
      --card-shadow: rgba(0,0,0,0.25);
    }

    html[data-theme="dark"] {
      --bg-page: #0f0f0f;
      --text-color: #f1f1f1;
      --card-bg: #1a1a1a;
      --input-bg: #222;
      --input-border: #555;
      --brand-color: #4da3ff;
      --button-bg: #4da3ff;
      --button-text: #fff;
      --card-shadow: rgba(0,0,0,0.6);
    }

    body.login-page {
      margin: 0;
      padding: 20px;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background: var(--bg-page);
      font-family: 'Segoe UI', Tahoma, sans-serif;
      transition: background 0.3s;
    }

    .form-container {
      width: 360px;
      padding: 30px;
      border-radius: 12px;
      background: var(--card-bg);
      box-shadow: 0 8px 24px var(--card-shadow);
      display: flex;
      flex-direction: column;
      gap: 15px;
      text-align: center;
    }

    h2 { color: var(--brand-color); margin-bottom: 20px; }

    input {
      width: 100%;
      padding: 12px;
      border-radius: 8px;
      border: 1px solid var(--input-border);
      background: var(--input-bg);
      color: var(--text-color);
      font-size: 15px;
      outline: none;
      transition: 0.2s;
    }

    input:focus { border-color: var(--brand-color); box-shadow: 0 0 6px rgba(0,123,255,0.3); }

    .forgot { text-align: right; margin-bottom: 10px; }
    .forgot a { color: var(--brand-color); font-size: 14px; text-decoration: none; }
    .forgot a:hover { text-decoration: underline; }

    button.btn-primary {
  width: 100%;            
  height: 44px;           
  background: var(--brand-color, #007bff); 
  color: #fff;            
  border: none;
  border-radius: 8px;
  font-weight: 600;
  font-size: 15px;        
  cursor: pointer;
  transition: 0.2s;
}

button.btn-primary {
  width: 100%;            /* mesma largura dos botões sociais */
  height: 44px;           /* mesma altura */
  background-color: #007bff; /* azul padrão igual ao início */
  color: #fff;            /* texto branco */
  border: none;
  border-radius: 8px;
  font-weight: 600;
  font-size: 15px;
  cursor: pointer;
  transition: 0.2s;
}

button.btn-primary:hover {
  opacity: 0.9;
  transform: translateY(-1px);
}

.social-btn.google { background-color: #db4437; } /* vermelho Google */
.social-btn.github { background-color: #24292e; } /* preto GitHub */
.social-btn { width: 100%; padding: 12px; border-radius: 8px; text-align: center; text-decoration: none; font-weight: 600; color: #fff; }


    .register-text { font-size: 14px; }
    .register-text a { color: var(--brand-color); font-weight: bold; text-decoration: none; }
    .register-text a:hover { text-decoration: underline; }

    .theme-btn {
      position: fixed; top: 20px; right: 20px;
      background: var(--brand-color); color: #fff; border: none;
      padding: 10px 14px; border-radius: 35px; cursor: pointer; font-size: 18px;
      box-shadow: 0 4px 10px var(--card-shadow); transition: 0.2s;
    }
    .theme-btn:hover { opacity: 0.85; transform: scale(1.05); }
  </style>
</head>

<body class="login-page">

<button id="themeToggle" class="theme-btn"><%= theme === "dark" ? "🌙" : "☀️" %></button>

<div class="form-container">
  <h2>Login</h2>

  <% if (user) { %>
    <p>Você já está logado como <%= user.name %>.</p>
    <a href="/" class="btn-primary">Ir para Home</a>
    <a href="/logout" class="btn-secondary">Sair</a>
  <% } else { %>
    <form action="/login" method="POST">
      <input type="email" name="email" placeholder="E-mail" required>
      <input type="password" name="password" placeholder="Senha" required>
      <div class="forgot"><a href="/forgot-password">Esqueci minha senha</a></div>
      <button type="submit" class="btn-primary">Entrar</button>
    </form>

    <div class="social-login">
      <a href="/auth/google" class="social-btn google">Entrar com Google</a>
      <a href="/auth/github" class="social-btn github">Entrar com GitHub</a>
    </div>

    <p class="register-text">Ainda não tem conta? <a href="/register">Registrar</a></p>
  <% } %>
</div>

<script>
  const btn = document.getElementById("themeToggle");
  btn.addEventListener("click", async () => {
    const html = document.documentElement;
    const current = html.getAttribute("data-theme") || "light";
    const next = current === "dark" ? "light" : "dark";
    html.setAttribute("data-theme", next);
    btn.textContent = next === "dark" ? "🌙" : "☀️";
    await fetch("/set-theme", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ theme: next }) });
  });
</script>

</body>
</html>
